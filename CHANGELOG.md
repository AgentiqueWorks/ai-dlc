# Changelog

## 0.2.0 (unreleased)

- Add `ai-dlc` CLI with `validate`, `install`, `init-repo`, and `mcp-sync`.
- Add governance hooks and `governance/settings.json`.
- Add `scripts/init-repo.py` for one-command project scaffolding.
- Add example evals for CSV export and payment audit.
- Add `pyproject.toml` and package metadata.
- Add `CONTRIBUTING.md`, issue templates, and release workflow.

## 0.1.0

- Initial release: 13 Agent Skills, 17 MCP configs, templates, examples, and README.