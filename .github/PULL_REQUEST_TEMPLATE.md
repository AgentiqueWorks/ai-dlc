## What changed

<!-- Brief summary -->

## Why

<!-- Motivation -->

## How to test

```bash
make
```

## Checklist

- [ ] `make` passes.
- [ ] New skills are in `skills/<name>/SKILL.md` with matching `name` and `description`.
- [ ] New MCP servers are in `mcp/configs/` and `make mcp-sync` was run.
- [ ] No secrets or credentials committed.